language: python
python:
  - "3.11"
stages:
  - execute

install:
  - curl -LsSf https://astral.sh/uv/install.sh | sh
  - export PATH="$HOME/.local/bin:$PATH"
  - uv sync --frozen

jobs:
  include:
    - stage: execute
      name: "Starting the Server"
      script: uv run python start_server.py
